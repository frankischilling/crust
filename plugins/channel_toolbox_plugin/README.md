# Channel Toolbox Plugin

This example shows:

- `c2.channel_by_name()`
- `c2.send_message()`
- `c2.add_system_message()`
- `c2.clear_messages()`
- `c2.register_callback()` for command completions

Try:

- `/chatbox info`
- `/chatbox note`
- `/chatbox say hello from Lua`
- `/chatbox clear`
