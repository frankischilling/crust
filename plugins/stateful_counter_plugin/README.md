# Stateful Counter Plugin

This example shows:

- `plugin_data_dir()` for persistence
- `c2.register_command()` for a small stateful tool
- `c2.register_callback()` for completions
- basic file I/O for saved plugin state

Try:

- `/counter`
- `/counter inc`
- `/counter note remember this`
- `/counter reset`
